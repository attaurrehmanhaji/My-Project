import 'package:flutter/material.dart' show AppBar, Center, Colors, MaterialApp, Scaffold, Text, runApp;

void main() {
  runApp(MaterialApp(
      home: Scaffold(
    backgroundColor: Colors.amber[400],
    appBar: AppBar(
      title: const Center(
        child: Text('Hello'),
      ),
    ),
    body: ,
  )));
}
